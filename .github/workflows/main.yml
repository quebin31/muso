name: Build release

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: rust:latest

    steps:
      - uses: actions/checkout@v2
      - name: Init SSH key and hosts
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_AUR_KEY }}
          name: aur
          known_hosts: ${{ secrets.KNOWN_HOSTS_AUR }}
      - name: Checkout pkgbuild submodule
        env:
          GIT_SSH_COMMAND: ssh -i $HOME/.ssh/aur
          #GIT_SSH_COMMAND: ssh -i $HOME/.ssh/aur -o StrictHostKeyChecking=no
        run: git submodule update --init --force --depth=1
      - name: Run tests
        run: cargo test
      - name: Build binary
        run: cargo build --release
      - name: Generate tarball
        run: tar czf release.tar.gz share target/release/muso
      - name: Update PKGBUILD
        run: |
          sha256sum=$(sha256sum release.tar.gz)
          version=$(echo $GITHUB_REF | cut -d '/' -f3)
          cd pkgbuild

          sed -i -E "s/(pkgver=).*/\1$version" PKGBUILD
          sed -i -E "s/(pkgrel=).*/\11" PKGBUILD
          sed -i -E "s/(sha256sums=).*/\1('$sha256sum')" PKGBUILD
      - name: Commit & push PKGBUILD
        uses: ./github/actions/push_pkgbuild
        with:
          sshkey: ${{ secrets.SSH_AUR_KEY }}
      - name: Upload tarball to release
        uses: yakuhzi/action-release@v1
        with:
          file: release.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
